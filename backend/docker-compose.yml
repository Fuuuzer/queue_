services:
  mysql:
    image: mysql:8
    container_name: mysql-ticket

    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: tickets

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
